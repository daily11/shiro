1 shiro流程：
1.1 ShiroConfig类中的shiroFilterFactoryBean方法
    此方法过滤拦截，其中一个是登录url拦截

1.2 UserRealm类中的doGetAuthenticationInfo(AuthenticationToken token)
    会进行登录验证，验证通过会将用户信息保存到shiro中

1.3 @RequiresPermissions("/rbac/bondAclModule")注解
    此注解一般应用在controller中

1.4 UserRealm类中的doGetAuthorizationInfo(PrincipalCollection arg0)
    获取权限并校验
